# Dwinity Tax Backend

`tax-api/` → deployed to `/opt/dwinity-tax-api/` · port 8082 · systemd unit `dwinity-tax-api.service`.
Secrets in `/etc/dwinity/tax.env`. SIWE login is provided by the shared identity-api
(repo: dwinity-drop, path server/identity-api).
